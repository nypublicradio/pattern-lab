{# {% if img.no_lazy %} #}
  <img src="{{ img.src }}" itemprop="image" alt="{{ img.alt }}" />
{#
{% else %}
  <img class="lazy" src="{{ img.src }}" itemprop="image" alt="{{ img.alt }}" />
{% endif %}
#}
